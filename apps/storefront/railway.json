{
  "$schema": "https://railpack.com/schema.json",
  "build": {
    "buildCommand": "corepack enable && pnpm install --frozen-lockfile && pnpm --filter @pipecommerce/storefront... build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @pipecommerce/storefront start",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
